# face

```bash
pip install pyTelegramBotAPI && git clone https://github.com/eternnoir/pyTelegramBotAPI.git && cd pyTelegramBotAPI && python setup.py install && cd .. && git clone https://github.com/sajjad-021/face.git && cd face && chmod 777 run.sh && screen -S nohup ./run.sh
```
